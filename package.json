{
  "name": "saga-proto-lexicon",
  "version": "0.0.1",
  "description": "Lexicon definitions for the Saga Proto AT Protocol game.",
  "main": "index.js",
  "scripts": {
    "validate": "lex-cli gen-ts-obj lexicons/ --out lexicons.d.ts",
    "build": "npm run validate"
  },
  "license": "MIT",
  "devDependencies": {
    "@atproto/lex-cli": "^0.9.0"
  }
}
